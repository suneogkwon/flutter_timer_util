name: flutter_timer_util
description: "A various timer utility package for debounce, throttle, repeat."
version: 0.0.1
repository: https://github.com/suneogkwon/flutter_timer_util

environment:
  sdk: '>=3.2.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
